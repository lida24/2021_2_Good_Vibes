{{!-- <div class="product-card">
  <a href="#" name="{{id}}">
    <div class="image">
      <img src="{{image}}" alt="{{name}}" />
    </div>
  </a>
  <div class="content">
    <h3><a href="#" name="{{id}}">{{name}}</a></h3>
    <div class="rating">
      rating
    </div>
    <div class="price">${{price}}/-</div>
  </div>
</div> --}}

<a href="#" name="{{id}}">
  <div class="image">
    <img src="{{image}}" alt="{{name}}" />
  </div>
</a>
<div class="content">
  <h3><a href="#" name="{{id}}">{{name}}</a></h3>
  <div class="rating">
    rating
  </div>
  <div class="price">${{price}}/-</div>
</div>