const searchParams: {
  minPrice: number,
  maxPrice: number,
  minRating: number,
  maxRating: number,
  type: 'asc' | 'desc',
} = {
  minPrice: 0,
  maxPrice: 0,
  minRating: 0,
  maxRating: 0,
  type: 'asc',
};

export default searchParams;
